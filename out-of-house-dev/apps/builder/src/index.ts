// apps/builder — headless Claude Code worker (Agent SDK) + Render deployer + PDF.
// Phase 0 scaffold only; the builder.run consumer, repo factory, preview envs,
// merge executor, renderDeploy and puppeteer PDF service arrive in Phase 6.

function main(): void {
  console.log('[builder] scaffold up — Agent SDK runner + deployer + PDF wired in Phase 6');
  setInterval(() => undefined, 1 << 30);
}

main();
