# Products — SaaS, Lead Engine, AISEO

Covers `/saas`, `/saas/logovault`, `/lead-engine`, `/aiseo`. ADOPT rules as elsewhere.

## `/saas` index

**Eyebrow:** SaaS & AIaaS · **H1:** Products we run, so you don't have to.

**Lead:**
> The same engineering we sell as a service, packaged as products. One is live in public beta; three more are on the bench. Everything here is built, hosted, and supported by the same senior team.

Cards from `saasApps.js` (ADOPT title, tagline/summary, statusLabel). Planned products render with their `statusLabel` and no CTA (no waitlist theatre unless a real waitlist exists).

## `/saas/logovault`

ADOPT every `saasApps.js#logovault` field verbatim (hero, offer, process, deliverables/pricing tiers, cta) — pricing (£0/£9/£39/£149) matches the price book exactly.

**One gated claim:** the hero proof point "900k+ logos indexed" — `[TODO:callum: confirm the real indexed count]`. If unconfirmed at publish, substitute: `{ kicker: 'SVG/PNG/JPG', value: 'every format' }` moves to slot one and the count is dropped. A checkable number we can't check doesn't ship.

## `/lead-engine`

ADOPT `leadgen.js` verbatim: hero, all seven pipeline stages, pricing tiers (they match the price book), cta — with **two changes**:

1. **Proof stat (CHANGED, per pricing_review §2):** replace `{ kicker: 'Cost / meeting', value: '~£15 (vs. ~£150 SDR cost)' }` with `{ kicker: 'Cost / meeting', value: 'a fraction of SDR cost' }` — the £15 figure is an unverified claim; reinstate a hard number only when cohort data backs it.
2. **Confidentiality check (passed, for the record):** the pipeline stages describe *what the engine does for the client* (discovery, scoring, drafting, booking) — outcomes, which is fine under §0.D. Phase B must not add any copy about how requests are processed internally.

## `/aiseo`

ADOPT `aiseo.js` verbatim — hero, all eight offer items, process, deliverables, all three pricing tiers, all four FAQs, cta. This page is the strongest copy on the site; the audit found nothing to change. Prices match the price book (£0 / £1,500+£500mo / £3,500+£1,500mo).
